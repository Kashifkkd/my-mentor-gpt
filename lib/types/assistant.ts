export interface AssistantType {
  id: string;
  name: string;
  description?: string;
  icon?: string;
  color: string;
}

export interface Workspace {
  id: string;
  name: string;
  slug: string;
  image?: string;
}

